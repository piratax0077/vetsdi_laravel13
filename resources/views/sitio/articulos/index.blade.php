@extends('sitio.layout')
@section('title','Mis artículos · VeterChile')
@section('content')
<section class="sw-config"><div class="sw-wrap"><header class="sw-config-hero"><div class="sw-section-head"><p class="sw-kicker">Contenido profesional</p><h1>Mis artículos</h1><p>Administra consejos y novedades que aparecerán públicamente en VeterChile.</p></div><a class="sw-btn sw-btn-solid" href="{{ route('sitio.articulos.create') }}">+ Nuevo artículo</a></header>@if(session('mensaje'))<p class="sw-flash">{{ session('mensaje') }}</p>@endif<div class="sw-article-admin-list">@forelse($articulos as $articulo)<article><div><span class="sw-status {{ $articulo->publicado?'':'sw-status-off' }}">{{ $articulo->publicado?'Publicado':'Borrador' }}</span><h2>{{ $articulo->titulo }}</h2><p>{{ $articulo->resumen }}</p><small>Actualizado {{ $articulo->updated_at->format('d-m-Y H:i') }}</small></div><nav><a class="sw-btn sw-btn-ghost" href="{{ route('sitio.articulos.edit',$articulo) }}">Editar</a>@if($articulo->publicado)<a class="sw-btn sw-btn-ghost" target="_blank" href="{{ route('sitio.articulos.show',$articulo->slug) }}">Ver</a>@endif<form method="POST" action="{{ route('sitio.articulos.destroy',$articulo) }}" onsubmit="return confirm('¿Eliminar este artículo?')">@csrf @method('DELETE')<button class="sw-btn sw-btn-danger">Eliminar</button></form></nav></article>@empty<p class="sw-card">Todavía no has creado artículos.</p>@endforelse</div></div></section>
@endsection
